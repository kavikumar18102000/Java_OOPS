package Interfaces.Static_in_interfaces;

public class Main {
    public static void main(String[] args) {

        /*
        we know that static methods do not depend on the objects, so we can access them by using their class or
        interface name as follows...
         */
        A.greet();
    }
}
